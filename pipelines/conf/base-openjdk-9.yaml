---
    dockerFileExists: true
    dockerFilePath: 'base/java/8/Dockerfile' # Local dockerfile path
    dockerImageLocation: '' # Remove container image path
    containerStructureTest: true
    containerStructureTestPath: 'base/java/8/tests'
    securityScan: true
    description: 'This will be used for running java application' # Mandatory
    source: 'hub.docker.com' # Mandatory
    md5DockerFile: '' # keep it empty it will be updated by pipeline Automatic update at first run after successful docker push to skip execution on second time.
